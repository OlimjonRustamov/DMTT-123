<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DMTT123" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowBackground">@color/black</item>
        <item name="android:colorPrimary">@color/black</item>
        <item name="android:datePickerDialogTheme">@style/DatePickerTheme</item>


        <item name="app_background_color">@color/black</item>
        <item name="app_text_color">@color/white</item>
    </style>
</resources>